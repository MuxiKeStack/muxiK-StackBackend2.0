syntax = "v1"

info(
	title: "用户中心"
	desc: "用户服务"
	author: "Barry Wu"
	email: "wyxnatural@gmail.com"
	version: "v1"
)

import (
	"user/user.api"
)

@server (
	prefix : api/v1/user
)

service usercenter{
	@doc "一站式登录"
	@handler login
	post /login (LoginRequest)returns(LoginResponse)
}